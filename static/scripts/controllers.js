// Controllers
'use strict';

app.controller('MainController', ['$scope',
  function ($scope) {
    $scope.title = 'Popcorn';
  }
]);

app.controller('HomeController', ['$scope',
  function ($scope) {
    $scope.title = 'Popcorn';
  }
]);
